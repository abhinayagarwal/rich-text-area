package com.gluonhq.richtext.viewmodel;

public class DecorateTextItalic extends DecorateTextCmd {

    public DecorateTextItalic() {
        // TODO
        /*state.addListener((o, ov, nv) -> {
            final Decoration decoration = Decoration.getInstance();
            decoration.setFontPosture(nv ? ITALIC : REGULAR);
        });*/
    }
}
